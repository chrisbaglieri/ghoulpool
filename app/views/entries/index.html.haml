.span-18.last
  %h2= "#{@pool.name} Entries"
  %hr
  .span-9
    %h3 Ghouls
    .entries
      = render :partial => "entry", :collection => @entries
    = paginate @entries
  .span9-last
    - if @entries.count < @pool.picks
      Hey you have empty slots! Get on that and 
      = link_to 'add more ghouls', new_pool_entry_path(@pool), :class => "small-button"