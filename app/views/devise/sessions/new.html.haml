= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
  %p
    = f.label :email
    = f.email_field :email
  %p
    = f.label :password
    = f.password_field :password
  %p= f.submit "Sign in", :class => "big-button"
= render :partial => "devise/shared/links"